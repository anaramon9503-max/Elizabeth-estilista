// La sección completa de servicios funciona como un único menú desplegable.
const menu = document.querySelector('.services-menu');
if (menu) {
  menu.addEventListener('toggle', () => {
    // Se deja preparado por si después quieres agregar animaciones o categorías.
  });
}
